# Interpolacion

Temas sembrados:

- lagrange
- newton

Nota:

- `punto_fijo` se documento en `raices_de_funciones` porque no es un metodo de
  interpolacion sino un metodo iterativo para ecuaciones no lineales.
